import os

cfactory_top = os.path.dirname(
        os.path.abspath(__file__)
        )
collections_path = os.path.join(
        cfactory_top,
        "assemblers"
        )
config_path = os.path.join(
        cfactory_top,
        "__config__"
        )
utils_path = os.path.join(
        cfactory_top,
        "utils"
        )
